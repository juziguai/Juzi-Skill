<!-- 初始化门禁：命中 Continuity 后，先创建或读取本文件，再执行任务调查、计划、委派、编辑或测试。 -->
<!-- 字段规则：创建/接管时显式初始化全部十个栏目；检查点只更新事件直接字段，并复核任务状态、未完成/阻塞、NEXT_ACTION。 -->

# 当前任务

## 目标与验收

- 目标：
- 验收：
- 任务状态：active / waiting_user / blocked / complete
- 初始化状态：新建 / 恢复 / 接管 / 迟建（迟建必须说明原因）
- 初始化时间：
- 触发原因：

## 关联 `/goal`

- 状态：未建立 / active / complete / blocked
- 建议：否 / 是（写明理由；仅建议，用户明确授权后才能创建）
- 目标：
- 关联说明：

## 团队协作与文件所有权

- 模式：单主 Agent / 只读并行 / 隔离写入并行 / 串行实施
- 主 Agent 写入范围：
- 子 Agent 租约：Agent 名称、允许文件、禁止文件、只读范围、验收命令、释放条件。
- 未认领改动处理：暂停相关写入，读取真实 diff 后由主 Agent 重新分配。

## 当前范围与禁区

- 允许修改：
- 禁止修改：

## 生产状态与破坏性边界

- 生产交互：否 / 只读 / 可逆 / 破坏性
- 最后只读核验：时间、命令/API 和结果
- 受保护基线：服务、父子进程/PID、端口、数据、配置、用户手动恢复状态
- 允许动作：
- 禁止动作：
- 破坏性动作预算：授权范围、已用次数、剩余次数、最近结果
- standby 就绪证据：
- 停止条件：
- rollback / rescue：

## 已证实事实与关键决定

-

## 已做改动

-

## 测试证据

- 命令：
- 结果：

## 未完成 / 阻塞

-

## NEXT_ACTION

<!-- active / waiting_user / blocked：恰好一个可验收动作；complete：必须且只能写 NONE。 -->

-
